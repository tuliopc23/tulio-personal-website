{
  "build_command": "SENTRY_RELEASE=$(git rev-parse HEAD) pnpm run build",
  "deploy_command": "pnpm exec wrangler deploy && echo $(git rev-parse HEAD) | pnpm exec wrangler secret put SENTRY_RELEASE"
}
