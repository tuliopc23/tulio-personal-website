---
import ArticleCard from "./ArticleCard.astro";
import PhosphorIcon from "./PhosphorIcon.astro";
import { isNewPublishedAt } from "../lib/is-new-post";
import { getAllPosts } from "../lib/content/posts";
import styles from "./RecentPosts.module.css";

const formatter = new Intl.DateTimeFormat("en", {
  month: "short",
  day: "numeric",
  year: "numeric",
});

// Fetch 3 most recent published posts
const recentPosts = (await getAllPosts()).slice(0, 3).map((post) => {
  const date = new Date(post.publishedAt);
  const summaryWordCount = post.summary.split(/\s+/).filter(Boolean).length;
  const estimatedReadingTime = Math.max(1, Math.round(summaryWordCount / 170));
  const isNew = isNewPublishedAt(post.publishedAt);

  return {
    ...post,
    href: `/blog/${post.slug}/`,
    dateISO: date.toISOString(),
    dateLabel: formatter.format(date),
    readingTimeMinutes: post.readingTimeMinutes ?? estimatedReadingTime,
    isNew,
  };
});

const hasPost = recentPosts.length > 0;
---

<section
  class:list={["container", styles.recentPosts]}
  data-reveal
  data-reveal-type="scale"
  data-reveal-group="recent-posts"
>
  <div class:list={[styles.header, "surface-header"]}>
    <h2 class:list={[styles.title, "surface-title"]}>Recent writing</h2>
    <p class:list={[styles.subtitle, "surface-caption"]}>
      Platforms, systems, tooling, and the decisions behind the code.
    </p>
  </div>

  {
    hasPost ? (
      <div class={styles.grid}>
        {recentPosts.map((post, index) => (
          <ArticleCard
            href={post.href}
            title={post.title}
            summary={post.summary}
            dateISO={post.dateISO}
            dateLabel={post.dateLabel}
            tags={post.tags}
            categories={post.categories}
            readingTimeMinutes={post.readingTimeMinutes}
            isNew={post.isNew}
            revealGroup="recent-posts"
            revealOrder={index}
          />
        ))}
      </div>
    ) : (
      <div class:list={[styles.empty, "surface-panel", "surface-panel--nested"]}>
        <h3>Nothing published yet</h3>
        <p>
          Posts are in draft. Add the{" "}
          <a href="/rss.xml" class={styles.link}>
            RSS
          </a>{" "}
          feed to your reader — first post will land there.
        </p>
      </div>
    )
  }

  <div class={styles.footer}>
    <a href="/blog" class:list={[styles.viewAll, "cta-pill", "cta-pill--neutral", "motion-cta-pill"]}>
      See all posts
      <PhosphorIcon name="arrow-right" size={16} weight="regular" />
    </a>
  </div>
</section>
