---
import Base from "../layouts/Base.astro";
import HeroPlayer from "../components/HeroPlayer";
import ProfileCard from "../components/ProfileCard.astro";
import DockLink from "../components/DockLink.astro";
import SectionQuickNav from "../components/SectionQuickNav.astro";
import GitHubLiveSection from "../components/github/GitHubLiveSection";
import FeatureWritingWidget from "../components/FeatureWritingWidget.astro";
import PhosphorIcon from "../components/PhosphorIcon.astro";
import { getAllPosts } from "../sanity/lib/posts";
import { getMergedGitHubData } from "../lib/github-data";

const posts = await getAllPosts();

// Pre-fetch GitHub data at build time so the widget renders instantly
let githubRepos: Awaited<ReturnType<typeof getMergedGitHubData>> = [];
try {
  githubRepos = await getMergedGitHubData(
    import.meta.env.GITHUB_TOKEN ?? import.meta.env.GITHUB_PERSONAL_ACCESS_TOKEN,
  );
} catch (err) {
  console.warn("Build-time GitHub fetch failed, widget will fetch client-side:", err);
}
const formatter = new Intl.DateTimeFormat("en", {
  month: "short",
  day: "numeric",
  year: "numeric",
});

const writingPosts = posts.slice(0, 3).map((post) => {
  const date = new Date(post.publishedAt);
  const summaryWordCount = post.summary.split(/\s+/).filter(Boolean).length;
  return {
    title: post.title,
    summary: post.summary,
    href: `/blog/${post.slug}/`,
    dateISO: date.toISOString(),
    dateLabel: formatter.format(date),
    readingTimeMinutes: post.readingTimeMinutes ?? Math.max(1, Math.round(summaryWordCount / 170)),
    tags: post.tags,
    categories: post.categories,
    heroImage: post.heroImage,
    coverVariant: post.coverVariant,
    isNew: Date.now() - date.getTime() < 7 * 24 * 60 * 60 * 1000,
  };
});

const toolLinks = [
  {
    title: "Ghostty",
    href: "https://ghostty.org",
    tint: "blue",
    iconAsset: "/icons/Tools/Ghostty Dark_upscayl_2x_digital-art-4x_wrapped.webp",
  },
  {
    title: "Neovim",
    href: "https://neovim.io",
    tint: "green",
    iconAsset: "/icons/Tools/neovimlight_upscayl_2x_digital-art-4x_wrapped.webp",
  },
  {
    title: "Xcode",
    href: "https://developer.apple.com/xcode/",
    tint: "blue",
    iconAsset: "/icons/Tools/xcodelogh_upscayl_2x_digital-art-4x.webp",
  },
  {
    title: "Containers",
    href: "https://support.apple.com/guide/terminal/welcome/mac",
    tint: "cyan",
    iconAsset: "/icons/Tools/Apple Containerization_upscayl_2x_digital-art-4x.webp",
  },
  {
    title: "Transmit",
    href: "https://panic.com/transmit/",
    tint: "yellow",
    iconAsset: "/icons/Tools/Transmit_upscayl_2x_digital-art-4x.webp",
  },
  {
    title: "Tower",
    href: "https://www.git-tower.com",
    tint: "orange",
    iconAsset: "/icons/Tools/Tower.webp",
  },
  {
    title: "Emacs",
    href: "https://www.gnu.org/software/emacs/",
    tint: "purple",
    iconAsset: "/icons/Tools/Emacs_LG3.svg",
  },
  {
    title: "OrbStack",
    href: "https://orbstack.dev",
    tint: "indigo",
    iconAsset: "/icons/orbstack.webp",
  },
  {
    title: "Things 3",
    href: "https://culturedcode.com/things/",
    tint: "blue",
    iconAsset: "/icons/Tools/Things3.webp",
  },
];

const stackLinks = [
  {
    title: "Swift",
    href: "https://developer.apple.com/swift/",
    tint: "orange",
    iconAsset: "/icons/stack/swift.svg",
  },
  {
    title: "Go",
    href: "https://go.dev",
    tint: "cyan",
    iconAsset: "/icons/stack/go.svg",
  },
  {
    title: "TypeScript",
    href: "https://www.typescriptlang.org",
    tint: "blue",
    iconAsset: "/icons/stack/typescript.svg",
  },
  {
    title: "React",
    href: "https://react.dev",
    tint: "blue",
    iconAsset: "/icons/stack/react.svg",
  },
  {
    title: "Vue",
    href: "https://vuejs.org",
    tint: "green",
    iconAsset: "/icons/stack/vuejs.svg",
  },
  {
    title: "Svelte",
    href: "https://svelte.dev",
    tint: "orange",
    iconAsset: "/icons/stack/svelte.svg",
  },
  {
    title: "Rust",
    href: "https://www.rust-lang.org",
    tint: "brown",
    iconAsset: "/icons/stack/rust.svg",
  },
  {
    title: "Zig",
    href: "https://ziglang.org",
    tint: "yellow",
    iconAsset: "/icons/stack/zig.svg",
  },
  {
    title: "Docker",
    href: "https://www.docker.com",
    tint: "indigo",
    iconAsset: "/icons/stack/docker.svg",
  },
];

const sectionNavItems = [
  { id: "section-profile", label: "Hello" },
  { id: "section-tools", label: "Tools" },
  { id: "section-stack", label: "Stack" },
  { id: "section-github", label: "Activity" },
  { id: "section-writing", label: "Writing" },
];

const siteOrigin = Astro.site?.origin ?? Astro.url?.origin ?? "https://www.tuliocunha.dev";
const canonical = new URL("/", siteOrigin).toString();
const description =
  "Tulio Cunha — full stack developer. Swift, TypeScript, Go, Rust, and Zig. Native apps, web systems, and the backend in between.";
---

<Base
  title="Home"
  description={description}
  canonical={canonical}
  ogDescription={description}
  ogImage={{ url: "/terminal-favicon.svg", alt: "Tulio Cunha portfolio" }}
  sidebar={false}
  mobileDrawer={true}
  heroPage={true}
>
  <section class="hero-remotion" aria-label="Hero">
    <div class="hero-remotion__player">
      <HeroPlayer client:only="react">
        <div class="hero-remotion__fallback" aria-hidden="true">
          <img src="/images/hero/macintosh-cleaned.webp" alt="" loading="eager" decoding="async" />
        </div>
      </HeroPlayer>
    </div>
    <div class="hero-remotion__scroll" aria-hidden="true">
      <span>Scroll</span>
      <span class="hero-remotion__scrollLine"></span>
    </div>
  </section>

  <section class="homepage-stage homepage-stage--hero-bridge" aria-labelledby="homepage-hello-title">
    <header class="stage-intro stage-intro--single">
      <div class="stage-intro__copy">
        <span class="stage-intro__eyebrow">
          <PhosphorIcon name="user" size={14} weight="regular" />
          Who am I
        </span>
        <h2 id="homepage-hello-title" class="stage-intro__title">
          Native to web and everything that runs in between.
        </h2>
        <p class="stage-intro__caption">
          Full stack developer. Swift and Apple platforms, TypeScript on the web, Go and Rust on the backend, Zig when it makes sense. I like software that works after the demo.
        </p>
      </div>
    </header>
  </section>

  <section id="top" class="bento-grid">
    <div id="section-profile" class="widget-profile">
      <ProfileCard
        name="Tulio Cunha"
        title="Full Stack Developer"
        location="Brazil"
        email="contact@tuliocunha.dev"
      />
    </div>

    <section class="homepage-stage homepage-stage--context" aria-labelledby="homepage-working-context-title">
      <header class="stage-intro">
        <div class="stage-intro__copy">
          <span class="stage-intro__eyebrow">
            <PhosphorIcon name="terminal-window" size={14} weight="regular" />
            Daily driver
          </span>
          <h2 id="homepage-working-context-title" class="stage-intro__title">
            The shell, the editor, and everything else I actually use.
          </h2>
          <p class="stage-intro__caption">
            My working environment for native apps, web systems, backend services, and the kind of yak-shaving that somehow always ships.
          </p>
        </div>

        <SectionQuickNav sections={sectionNavItems} />
      </header>

      <div class="widget-carousel widget-carousel--context" role="region" aria-label="Daily setup widgets">
        <div
          id="section-tools"
          class="widget-box widget-tools surface-panel surface-panel--premium hover-elevate"
          data-reveal
          data-parallax-card
        >
          <div class="widget-header surface-header">
            <span class="widget-eyebrow surface-eyebrow">
              <PhosphorIcon name="toolbox" size={13} weight="regular" />
              Toolkit
            </span>
            <h2 class="widget-title surface-title">
              <span>Tools</span>
              <PhosphorIcon name="toolbox" size={18} weight="regular" />
            </h2>
            <p class="widget-caption surface-caption">The stuff that stays open. Terminals, editors, and a few apps worth paying for.</p>
          </div>
          <ul class="tools-grid" aria-label="Tools">
            {toolLinks.map((link, index) => (
              <DockLink
                href={link.href}
                title={link.title}
                tint={link.tint}
                iconAsset={link.iconAsset}
                revealOrder={index}
                sizeVariant="dock-premium"
                imageLoading="eager"
              />
            ))}
          </ul>
        </div>

        <div
          id="section-stack"
          class="widget-box widget-stack surface-panel surface-panel--premium hover-elevate"
          data-reveal
          data-parallax-card
        >
          <div class="widget-header surface-header">
            <span class="widget-eyebrow surface-eyebrow">
              <PhosphorIcon name="code" size={13} weight="regular" />
              Languages
            </span>
            <h2 class="widget-title surface-title">
              <span>Stack</span>
              <PhosphorIcon name="code" size={18} weight="regular" />
            </h2>
            <p class="widget-caption surface-caption">What I write code in. The right tool depends on where the binary ends up.</p>
          </div>
          <ul class="tools-grid" aria-label="Stack">
            {stackLinks.map((link, index) => (
              <DockLink
                href={link.href}
                title={link.title}
                tint={link.tint}
                iconAsset={link.iconAsset}
                revealOrder={index}
                sizeVariant="dock-premium"
                iconContext="stack"
                imageLoading="eager"
              />
            ))}
          </ul>
        </div>
      </div>
    </section>

    <section class="homepage-stage" aria-labelledby="homepage-proof-title">
      <header class="stage-intro stage-intro--single">
        <div class="stage-intro__copy">
          <span class="stage-intro__eyebrow">
            <PhosphorIcon name="chart-bar" size={14} weight="regular" />
            Open source
          </span>
          <h2 id="homepage-proof-title" class="stage-intro__title">What moved recently and where the commits landed.</h2>
          <p class="stage-intro__caption">
            Live from the git log. Repos, contributions, and whatever I pushed this week.
          </p>
        </div>
      </header>

      <GitHubLiveSection client:load initialData={githubRepos.length > 0 ? githubRepos : undefined} />
    </section>

    <section class="homepage-stage" aria-labelledby="homepage-exploration-title">
      <header class="stage-intro stage-intro--single">
        <div class="stage-intro__copy">
          <span class="stage-intro__eyebrow">
            <PhosphorIcon name="article" size={14} weight="regular" />
            Journal
          </span>
          <h2 id="homepage-exploration-title" class="stage-intro__title">Long-form notes on things I actually built.</h2>
          <p class="stage-intro__caption">
            Platform deep dives, tooling opinions, architecture decisions, and the kind of writeups that start with "I tried this and here's what happened."
          </p>
        </div>
      </header>

      <div
        id="section-writing"
        class="widget-box widget-feature-writing surface-panel surface-panel--premium hover-elevate"
        data-reveal
        data-parallax-card
      >
        <div class="widget-header surface-header">
          <span class="widget-eyebrow surface-eyebrow">
            <PhosphorIcon name="article" size={13} weight="regular" />
            Journal
          </span>
          <h2 class="widget-title surface-title">
            <span>Writing</span>
            <PhosphorIcon name="article" size={18} weight="regular" />
          </h2>
          <p class="widget-caption surface-caption">Recent posts on Swift, web stacks, systems programming, and the tools around them.</p>
        </div>
        <FeatureWritingWidget posts={writingPosts} />
      </div>
    </section>
  </section>

</Base>
